{
  "name": "@tkmp-interview/util",
  "version": "0.1.0",
  "types": "./lib",
  "main": "./lib",
  "private": true,
  "dependencies": {
    "@types/node": "^12.0.0",
    "@types/react": "^17.0.0",
    "copyfiles": "^2.4.1",
    "error-overlay-webpack-plugin": "^0.4.2",
    "react": "^17.0.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "build": "node ./build.js && yarn run compile",
    "compile": "tsc",
    "watch": "tsc -w",
    "postinstall": "yarn run compile"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
